// WIC Bat-Speed Engine — owns the rotational-power pool.
// Prefer elastic_rotation-category slugs first so the certifier's canonical
// bs.max / bs.elastic templates (which require `elastic_rotation`) are
// always satisfied. Med-ball options remain available as secondary picks.
export const BAT_SPEED_PREFERRED = [
  "band_resisted_swings",
  "cable_chops",
  "med_ball_shot_put",
];
