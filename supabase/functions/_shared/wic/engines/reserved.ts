// WIC engines that are declared in the constitution but not yet
// authoring blocks in wic_v1. Reserved so the generator, validator, and
// UI can reference them without introducing empty slug pools.
export const RESERVED_ENGINE_NOTE = "reserved — no v1 emissions";
