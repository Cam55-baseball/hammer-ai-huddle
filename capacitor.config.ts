import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.hammersmodality.app',
  appName: 'Hammers Modality',
  webDir: 'dist'
};

export default config;
