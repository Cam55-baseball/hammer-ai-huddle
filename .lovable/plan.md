

# Replace Baseball Glove with Shield Icon

Swap `BaseballGloveIcon` for Lucide's `Shield` icon in two files:

### 1. `src/components/AppSidebar.tsx`
Replace `BaseballGloveIcon` import/usage with `Shield` from `lucide-react` for the Drill Library nav item.

### 2. `src/pages/DrillLibraryPlayer.tsx`
Replace all `BaseballGloveIcon` usages (header + empty state) with `Shield` from `lucide-react`.

No new components needed — `Shield` is a standard Lucide icon.

