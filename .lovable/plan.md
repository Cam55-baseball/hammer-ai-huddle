# Phase 28 — Canonical Base Running Intelligence System, Risk Timing Architecture & Dynamic Field Pressure Doctrine

Constitutional memory-only phase. Two file touches. No code, UI, runtime, migrations, analytics.

## File Touch 1 — Create

`mem://architecture/asb-phase-28-canonical-base-running-intelligence-system-risk-timing-architecture-dynamic-field-pressure-doctrine.md`

Full §A–§J constitutional depth:

- §A Core Domain Doctrine — base running as ASB-governed risk-timing execution under dynamic defensive pressure; organism-state determines initiation/aggression/hesitation/return probability; not speed, not instinct, not experience.
- §B Functional Subsystem Architecture — lead intelligence (distance + timing window compression) + steal decision engine (risk vs success under ASB) + advancement logic (ball-in-play + fielder state inference) + retreat/return control (containment under uncertainty collapse).
- §C ASB-to-Base-Running Mapping — readiness→lead stability+initiation clarity; fatigue→late breaks+hesitation+return delay; missingness→misread arm/ball uncertainty; confidence→advancement aggression threshold; load→reduced burst+slower recovery decision loop.
- §D Decision / Execution Model — latency-bound risk evaluations under unfolding field state; invalid if initiation latency exceeds throw window / fatigue collapses perception / missingness prevents reliable field-state inference.
- §E Timing & Risk Pressure Architecture — throw timing compression + ball travel uncertainty + fielder reaction latency + runner acceleration curve under fatigue; pressure increases exponentially with defensive proximity and uncertainty overlap.
- §F Failure Modes — early break → forced out spike / late break → lost opportunity / misread arm → dead-out / fatigue collapse → hesitation loop; fallback conservative hold + containment priority.
- §G Developmental Progression Model — Stage 1 reactive advancement → Stage 2 predictive timing-based advancement → Stage 3 autonomous field-pressure exploitation; organism-state governed, not training volume.
- §H Cross-System Integration — depends on hitting (ball-in-play), pitching (defensive setup), fielding (throw capability), catcher (relay coordination); downstream of full field ASB state graph.
- §I Observability & Audit — lead distance + initiation timing + break decision + advancement path + retreat decision all ASB-reconstructable; no hidden intent inference.
- §J Immutable Invariants BR-1…BR-10 — state-driven risk execution / timing governs success ceiling / fatigue distorts initiation before speed / missingness increases misread risk / dynamic field pressure constraint / no isolated runner decisions / ASB governs advancement validity / state-driven development / cross-field coordination required / replay-safe ASB-consistent execution.

## File Touch 2 — Update `mem://index.md`

1. Prepend ONE new Memories entry referencing Phase 28 (above all existing entries).
2. Append 8 Core lines: ASB-governed base running risk-timing intelligence; dynamic field pressure + throw window compression; steal decision architecture under organism-state; lead timing + initiation latency doctrine; fatigue-sensitive advancement/retreat control; cross-system dependency (hit/pitch/field/catch); developmental progression under ASB stability; replay-safe ASB-consistent execution framework.
3. Preserve ALL prior content verbatim.

## Out of Scope

No code, UI, migrations, runtime, analytics, deployment.

## Next Phase

Phase 29 — TBD.
