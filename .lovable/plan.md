## Changes

**`src/components/dashboard/WeeklyDigestPreview.tsx`**
- Remove the `Full recap →` link from the header (header now contains only the icon + "Weekly Recap" title, giving it full width).
- After the bullets `<ul>`, add the `Full recap →` link as a footer below the list.

**`src/components/dashboard/ForecastPreview.tsx`**
- Change title text from `What's Likely Next` to `What's Next`.
- Remove the `See more →` link from the header (header now shows only icon + "What's Next").
- After the bullets `<ul>`, add the `See more →` link as a footer below the list.

No data, hooks, layout grid, or other component changes.
